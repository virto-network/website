<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta name="description" content="{{ site.description }}">
	<meta name="theme-color" content="#3A3387">
	<title>{{ page.title }}</title>
	
	<style>
	:root { --color-accent: darkslateblue; }
	html, body { all: initial; font-family: sans-serif; }
	main { max-width: 900px; margin: auto; padding: 2rem 1rem; }
	nav { height: 4rem; margin-bottom: 2rem; }
	footer { height: 5rem; background: whitesmoke; }
	header h1, header p { text-align: center; }
	header h1 { color: var(--color-accent); }
	header p { font-style: italic; } 
	header time::after { content: '⬣'; margin: 0 1em; color: var(--color-accent); }
	header picture {
		background: var(--color-accent);
		color: white;
		display: block;
		margin: 3rem 0;
		padding: 1rem; border-radius: 5px;
		width: 100%; height: 20vh;
		box-sizing: border-box;
	}
	article { line-height: 1.6em; max-width: 700px; margin: auto; }
	:is(article) a, strong { color: var(--color-accent); }
	article ul { list-style: "⬡   "; }
	p { color: darkslategray; }
	blockquote {
		background: whitesmoke;
		border-radius: 5px;
		margin: 2rem 0; padding: 1rem;
	}
	blockquote p { margin: 0; color: dimgray; }
	</style>
</head>

<body>
	<nav></nav>
	<main>
		<header>
			<h1>{{ page.title }}</h1>
			<p>
				<time datetime="{{ page.published_date}}">{{ page.published_date | date: "%b %d, %Y" }}</time>
				by {{ page.data.author }}
			</p>
			<picture>
				<img src="" alt="Post cover">
			</picture>
		</header>
		<article>{{ page.content }}</article>
	</main>
	<footer></footer>
</body>
</html>

