---
permalink: /servicios/
title: Servicios
layout: index.liquid
data:
  route: servicios
---
<style>
	#nav { background-color: var(--clr-blue-light); }
	#nav a:not(#about-link) { color: var(--clr-olive-darker); }
	#menu-dropdown a { color: pink !important; }
	#menu-icon svg line { stroke: var(--clr-olive-darker); }
	#virto-logo svg path { fill: var(--clr-olive-darker); }

	#services { height: 80vh; display: flex; align-items: center; justify-content: center; gap: 10rem; }
	#services h1 { position: relative; left: 4rem; }
	#services h1::before {
		content: url('../img/colmenas.svg');
		display: relative;
		width: 150px;
		display: inline-block;
		position: absolute;
		left: -7rem;
		top: -5rem;
	}
	#services div { display: flex; flex-direction: column; gap: 2rem; max-width: 800px; }

	#services-list { flex-direction: column; flex-wrap: wrap; justify-content: space-between;}
	
	img { 
		width: 120px;
	}
	#services-list ul {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		grid-template-rows: auto auto;
		gap: 2rem;
		padding-top: 3rem;
		list-style: none;
	}

	ul li:last-child {
		grid-row: 2;
		grid-column: span 2;
	}

	ul li { 
		padding: 2rem;
		border-radius: 5px;
		display: flex;
		justify-content: space-between;
	}
	.text-container h4 { margin-bottom: 1rem; }
	.text-container h5 { line-height: 1.5; padding-right: 1rem; }

	@media (max-width: 750px) {
		section { flex-direction: column; }
		#services h1 { position: relative; top: 1.2rem; }
		#services-list ul { display: flex; flex-direction: column; flex-wrap: wrap; }
		ul li { flex-direction: column; }
		img { align-self: center; margin-top: 1rem; }
	}
</style>
	<section id="services" class="light-blue-bg">
		<h1>Servicios</h1>
		<div>
			<h3 class="regular">Sumergirse en la Web 3.0 puede parecer abrumador, pero no estás solo en este viaje.</h3>
			<h3 class="regular">En Virto, no solo te proporcionamos las herramientas; te acompañamos y guiamos en cada paso para <span class="bolder dark-lavender-text">adaptar tu negocio al nuevo horizonte digital.<span></h3>
		</div>
	</section>
	<section id="services-list">
		<h2 class="bold white-text">Servicios esenciales de Virto</h2>
		<ul>
				{% for service in site.data.index.services %}
				<li class="light-blue-bg dark-olive-text">
					<div class="text-container">
						<h4 class="bolder">{{ service.title }}</h4>
						<h5 class="bold">{{ service.description }}</h5>
					</div>
					<img src="{{ service.icon.url }}" alt="{{ service.icon.alt }}"></img>
				</li>
			{% endfor %}
		</ul>
	</section>

