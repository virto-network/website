---
title: Valibre
layout: default.liquid
data:
  caption: Decentralization for the masses
---

<style>
@keyframes flap {
	0% { transform: rotateY(0) }
	50% { transform: rotateY(0) }
	51.5% { transform: rotateY(25deg) }
	53% { transform: rotateY(10deg) }
	54% { transform: rotateY(10deg) }
	56% { transform: rotateY(30deg) }
	58% { transform: rotateY(0) }
	100% { transform: rotateY(0) }
}
#logo svg { width: 60vmin; margin: auto; max-width: 500px; display: block; }
#logo:hover path { animation: none; }
#logo path {
	animation: flap 10s linear infinite 500ms;
	transition: transform 400ms;
	transform-origin: center;
}
#logo path:hover { transform: rotateY(25deg); }
#logo figcation { text-align: center; font-weight: bold;  }
#logo h2 { font-size: 2em; margin-bottom: 0.3rem; }
#logo p { color: rgba(255, 255, 255, 0.8); font-size: 0.9em; margin-top: 0.3rem; }
</style>

<figure id="logo" title="Valibre">
	<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 63.5 60.21">
		<path d="m 30.116,31.47 v 23.45 L 5.3,28.74 V 5.29 Z" style="fill:#41e506" />
		<path d="m 33.39,31.47 v 23.45 l 24.8,-26.19 V 5.29 Z" style="fill:#46f805" />
	</svg>
	<figcation>
		<h2>{{ page.title }}</h2>
		<p>{{ page.data.caption }}</p>
	</figcation>
</figure>

